// Não importa onde as variaveis sejam declaradas dentro de um escopo
// Elas sempre serão movidas para o topo do escopo seja ele global ou local
// Ou seja as declarações das variaveis são inçadas para o topo
// Para essa situação e somente movida a declaração

let pais // Declarando
pais = 'Brasil' // Atribuição
console.log(pais)